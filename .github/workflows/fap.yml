name: Build FAP

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Upload placeholder
        uses: actions/upload-artifact@v4
        with:
          name: test
          path: README.md
